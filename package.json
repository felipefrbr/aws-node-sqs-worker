{
  "name": "aws-node-sqs-worker-project",
  "version": "1.0.0",
  "description": "Serverless Framework Node SQS Producer-Consumer on AWS",
  "author": "",
  "license": "MIT",
  "scripts": {
    "start:local": "serverless offline start --stage local --region us-east-1",
    "start:debug": "SLS_DEBUG=* serverless offline start --stage local --region us-east-1",
    "docker:up": "docker-compose up",
    "docker:down": "docker-compose down"
  },
  "dependencies": {
    "aws-sdk": "^2.1167.0",
    "serverless": "^3.19.0",
    "serverless-offline": "^8.8.0",
    "serverless-offline-sqs": "^6.0.0"
  }
}
